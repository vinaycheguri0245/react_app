import React from "react";

// import { withTheme as Form } from "pie-json-schema-form";

function FormRenderer({ config }) {
  const formTheme = "cerulean";
  return (
    <div>
      {/* <Form
        schema={config.schema}
        uiSchema={config.uiSchema}
        formTheme={formTheme}
      ></Form> */}{" "}
      Loading React app
    </div>
  );
}
export default FormRenderer;
